To get it up and running please open TweetSaver.html in a browser.

The application is built with HTML5, CSS and JavaScript libraries.

The following libraries were used:
 - KnockoutJS library -- it easily associates DOM elements with model data using a concise, readable syntax;
 - jQuery with a purpose of parsing DOM and making AJAX requests;
 - jQueryUI drag-and-drop components;
